{
  "name": "CSE197Dashboard",
  "version": "1.0.0",
  "description": "## Version 1 Everything will be based on CSV.",
  "main": "./src/main.js",
  "bin": "./src/bin.js",
  "scripts": {
    "start": "npm run build && ./dist/bundle-macos",
    "build": "rollup --config ./rollup.config.js --file ./dist/src/bundle.js --format cjs && pkg ./dist/src/bundle.js --out-path ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Andrew Kuo",
  "license": "MIT",
  "dependencies": {
    "papaparse": "^5.0.0"
  },
  "devDependencies": {
    "pkg": "^4.4.0",
    "rollup": "^1.19.4",
    "rollup-plugin-terser": "^5.1.1"
  }
}
