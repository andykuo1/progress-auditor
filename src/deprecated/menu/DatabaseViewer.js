import * as Menu from './Menu.js';

/**
 * This will run the steps to make view the database.
 * @param {Object} opts Any additional options.
 */
export async function run(opts = {})
{
    console.log("Hello.");
}